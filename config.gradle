ext {
    def major = 0
    def minor = 0
    def patch = 1

    buildConfig = [
        "minSdk"    : 28,
        "compileSdk": 33,
        "targetSdk" : 33
    ]
    releaseConfig = [
        // TODO - com.infinum.<YOUR-AWESOME-LIBRARY-NAME>
        "group"      : "com.infinum.libname",
        "version"    : "$major.$minor.$patch",
        "versionCode": major * 100 * 100 + minor * 100 + patch
    ]
}